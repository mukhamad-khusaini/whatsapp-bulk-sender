<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Bulk Chat Whatsapp</title>
        <style>
            * {
                box-sizing: border-box;
                margin: 0;
                padding: 0;
            }

            form {
                width: 100%;
                height: 100vh;
                display: flex;
                justify-content: center;
                align-items: center;
                flex-direction: column;
            }

            form label {
                margin-top: 2rem;
            }

            form input {
                display: block;
                text-align: center;
                width: 20rem;
                height: 2rem;
                outline: none;
                /* border: none; */
                padding: 2px 2px;
            }

            form button {
                padding: 0.6rem 2rem;
                background-color: greenyellow;
                border: none;
                font-weight: bold;
                border-radius: 5px;
                margin-top: 2rem;
                cursor: pointer;
            }

            form button:hover {
                background-color: rgb(120, 179, 32);
            }
        </style>
    </head>
    <body>
        <form action="chat" method="post">
            <label for="number">Input Number (coma sparated)</label>
            <input type="text" id="number" name="number" />
            <label for="text">Input Message</label>
            <input type="text" id="text" name="message" />

            <button type="submit">Send Message!</button>
        </form>
    </body>
</html>
